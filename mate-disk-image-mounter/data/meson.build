i18n.merge_file(
  input: 'mate-disk-image-mounter.desktop.in',
  output: 'mate-disk-image-mounter.desktop',
  type: 'desktop',
  po_dir: join_paths(meson.project_source_root(), 'po'),
  install: true,
  install_dir: applicationsdir,
)
